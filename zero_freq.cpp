#include <bits/stdc++.h>
using namespace std;
#define LL long long
#define endl "\n"

int f(int ind, vector<LL> &v, vector<int> &dp, bool has)
{
    if (ind >= 0)
        return 0;
    
}

void solve()
{
    int n;
    cin >> n;
    vector<LL> v(n);
    for (auto &val : v)
        cin >> val;
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}